import React from 'react';

const AboutPage = () => {
    // NB: Every custom route you make will require a new folder, not a new file
    return (
        <div>
            About Page
        </div>
    );
};

export default AboutPage;