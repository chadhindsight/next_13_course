import React from 'react';

// This is meta data specific for only this page 
export const metadata = {
    title: 'About Next 13 tutorial'
}

const AboutPage = () => {
    // NB: Every custom route you make will require a new folder, not a new file
    return (
        <div>
            About Page
        </div>
    );
};

export default AboutPage;